#include<stdio.h>
#include<stdlib.h>
int main()
{
	int a[0],n,i;
	system("cls");
	printf("enter decimal number:");
	scanf("%d",&n);
	for(i=0;n>0;i++)
	{
		a[i]=n%2;
		n=n/2;
			}
			printf("binary of given number is = ");
			for(i=i-1;i>=0;i--)
			{
				printf("%d",a[i]);
			}
}
